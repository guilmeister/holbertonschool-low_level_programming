#include "holberton.h"

/**
 * _isalpha - Checks for lowercase
 *
 * @c: value from main
 *
 * Return: Always 0 (Success)
 */

int _isalpha(int c)
{


if (isalpha(c))
return (1);

else
return (0);

}

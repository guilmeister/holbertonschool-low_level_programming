#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{

int array[10] = {'H', 'o', 'l', 'b', 'e', 'r', 't', 'o', 'n'};
int x;

for (x = 0; x <= 8; x++)
_putchar(array[x]);

_putchar('\n');
return (0);
}
